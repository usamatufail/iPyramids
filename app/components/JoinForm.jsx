import {Widget} from '@typeform/embed-react';

export const JoinForm = () => {
  return (
    <Widget
      id="uusXuRx1"
      autoFocus
      chat
      style={{height: '600px', width: '100%'}}
      //   style={{ width: '50%' }} className="my-form"
    />
  );
};
